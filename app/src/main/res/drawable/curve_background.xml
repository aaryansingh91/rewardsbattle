<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/red" /> <!-- Change to your desired background color -->
    <corners android:radius="8dp" /> <!-- Adjust the radius for the curve -->
    <stroke
        android:width="1dp"
        android:color="@color/red" /> <!-- Optional: Adds a border -->
</shape>
