<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/cardBg" /> <!-- Change to your desired background color -->
    <corners android:radius="10dp" /> <!-- Adjust the radius for the curve -->
    <stroke
        android:width="2dp"
        android:color="@color/pink" /> <!-- Optional: Adds a border -->
</shape>
