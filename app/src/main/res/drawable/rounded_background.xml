<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/pink" /> <!-- Change to your desired background color -->
    <corners android:radius="20dp" /> <!-- Adjust the radius for the curve -->
    <stroke
        android:width="1dp"
        android:color="@color/pink" /> <!-- Optional: Adds a border -->
</shape>
